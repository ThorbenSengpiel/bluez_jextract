// Generated by jextract

package bluez.binding;

import java.lang.invoke.MethodHandle;
import java.lang.invoke.VarHandle;
import java.nio.ByteOrder;
import jdk.incubator.foreign.*;
import static jdk.incubator.foreign.ValueLayout.*;
public interface GPollableSourceFunc {

    int apply(MemoryAddress pollable_stream, MemoryAddress user_data);
    static NativeSymbol allocate(GPollableSourceFunc fi, ResourceScope scope) {
        return RuntimeHelper.upcallStub(GPollableSourceFunc.class, fi, constants$495.GPollableSourceFunc$FUNC, "(Ljdk/incubator/foreign/MemoryAddress;Ljdk/incubator/foreign/MemoryAddress;)I", scope);
    }
    static GPollableSourceFunc ofAddress(MemoryAddress addr, ResourceScope scope) {
        NativeSymbol symbol = NativeSymbol.ofAddress("GPollableSourceFunc::" + Long.toHexString(addr.toRawLongValue()), addr, scope);
return (MemoryAddress _pollable_stream, MemoryAddress _user_data) -> {
            try {
                return (int)constants$496.GPollableSourceFunc$MH.invokeExact(symbol, (Addressable)_pollable_stream, (Addressable)_user_data);
            } catch (Throwable ex$) {
                throw new AssertionError("should not reach here", ex$);
            }
        };
    }
}


